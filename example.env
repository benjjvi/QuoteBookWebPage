# BASE CONFIG SETTINGS
SECRET_KEY = "FLASK_SECRET_KEY"
IS_PROD = 0
HOST = "127.0.0.1"
PORT = "8040"
PUBLIC_BASE_URL = "https://example.com"
OPENROUTER_KEY = "OPENROUTER_API_KEY"
ROBOTS_DISALLOW_ALL = "false"

# APP CONFIG SETTINGS
EDIT_PIN = "1234"
QUOTEBOOK_DB = "qb.db"
APP_MODE = "CLIENT"
# In run.py client mode: true = start local API + web app (split standalone)
# In direct app.py run: true = local DB mode
APP_STANDALONE = "true"

# CLIENT MODE CONFIG SETTINGS
QUOTE_API_URL = "http://127.0.0.1:8050"

# SERVER MODE CONFIG SETTINGS
API_HOST = "0.0.0.0"
API_PORT = "8050"
CORS_ORIGIN = "*"

# VAPID CONFIG SETTINGS
VAPID_PUBLIC_KEY="PUBLIC_VAPID_KEY"
VAPID_PRIVATE_KEY="PRIVATE_VAPID_KEY"
VAPID_EMAIL=mailto:you@example.com

# WEEKLY EMAIL DIGEST (Monday 07:00 UK)
WEEKLY_EMAIL_ENABLED=false
WEEKLY_EMAIL_FROM="quotes@example.com"
WEEKLY_DIGEST_SPONSOR_LINE="Sponsored by Acme Co. - https://example.com"
WEEKLY_SCHEDULER_MODE="auto" # auto=thread (most hosts) / external (PythonAnywhere)
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="smtp_username"
SMTP_PASS="smtp_password"
SMTP_USE_TLS=true
SMTP_USE_SSL=false
# Optional one-time seed into qb.db table `weekly_email_recipients`
# WEEKLY_EMAIL_TO_SEED="friend1@example.com,friend2@example.com"

# MONETISATION (OPTIONAL)
SUPPORT_URL="https://buymeacoffee.com/yourname"
SUPPORT_LABEL="Support Quote Book"
SPONSOR_CONTACT_URL="https://example.com/sponsor"
SPONSOR_CONTACT_EMAIL="sponsor@example.com"
AFFILIATE_DISCLOSURE="Disclosure: Some links may be affiliate links."
# AdSense config (optional)
ADSENSE_CLIENT_ID="ca-pub-xxxxxxxxxxxxxxxx"
ADSENSE_SLOT_INLINE="0000000000"
ADSENSE_SLOT_FOOTER="0000000000"
GOOGLE_ADSENSE_ACCOUNT="ca-pub-xxxxxxxxxxxxxxxx"

# Optional project root override for pythonanywhere_wsgi.py (if not set, defaults to /home/you/qb)
QUOTEBOOK_PROJECT_ROOT = "/path/to/your/project/root"
WSGI_TARGET = "api" # API only serves the API, anything else serves the full app (API + web app)
